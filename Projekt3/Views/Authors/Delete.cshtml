@model Projekt3.Models.Author

@{
    ViewData["Title"] = "Delete the author";
}

<h2>Delete the author</h2>

<p class="text-danger">Are you sure you want to delete this author?</p>
    <div>
      
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Name)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Name)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.LastName)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.LastName)
            </dd>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="authorId" />
            <input type="submit" value="Delete" class="btn btn-success" /> 
            <br />
            <br />
            <a asp-action="Index" class="previous"> &larr;Back to List</a>
        </form>
    </div>
